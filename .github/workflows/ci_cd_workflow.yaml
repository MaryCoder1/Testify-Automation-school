name: GitHub Actions Demo

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v3

      - name: Announce the trigger
        run: echo "🚀 The job was automatically triggered by a ${{ github.event_name }} event."

      - name: Run main.py
        run: python main.py
